# httpclient

Binaries for Windows and Linux can be found in `runtimes` folder. Copy this folder to the same directory as the executable so it can be found when the program runs.

# TLDR
Just use curl.